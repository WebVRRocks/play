detecting = <span data-l10n-id="detecting">Detecting &hellip;</span>

os = Operating System
os_value = <strong data-os-name="{ $name }">{ $name }</strong> <span data-os-version="{ $version }">{ $version }</span>

browser = Browser
browser_value = <strong data-browser-name="{ $name }">{ $name }</strong> <span data-browser-version="{ $version }">{ $version }</span>

device = Device
device_value = <strong data-device-vendor="{ $vendor }">{ $vendor }</strong> <span data-device-model="{ $model }">{ $model }</span> <span data-device-type="{ $type }">{ $type }</span>
